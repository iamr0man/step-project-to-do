class Note extends Api {
    constructor(list){
        super();
        this.list = list;
    }
    createNote() {
            
        return window.location.href= `${window.location.href}notes`;
    }
}
